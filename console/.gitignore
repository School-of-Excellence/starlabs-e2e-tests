# Angular / build
/dist
/tmp
/out-tsc
/.angular/cache
node_modules

# Local environment file with real keys — never commit (copy from firebase.config.example.ts)
/src/environments/firebase.config.ts

# Firebase tooling
.firebase
*-debug.log
