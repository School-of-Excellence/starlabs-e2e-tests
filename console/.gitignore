# Angular / build
/dist
/tmp
/out-tsc
/.angular/cache
node_modules

# Local Firebase web config (real keys) — never commit; use firebase.config.example.ts
/src/environments/firebase.config.ts

# Firebase tooling
.firebase
*-debug.log
