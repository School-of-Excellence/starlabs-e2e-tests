// events-chat-controls-addressable.spec.ts — ADDRESSABLE + SMOKE coverage for the Events Chat screens'
// interactive controls (Interactive-Control Coverage Program,
// specs/plans/2026-09-14-interactive-control-coverage-plan.md).
//
// Screens (all default-DB, NOT ATC-fenced):
//   /group-chat        -> Events/Chat/chat-screen           (prefix chs)
//   /group-chat-screen -> Events/Chat/group-chat-screen     (prefix gcs)
//   create-group-dialog (dialog opened from chat-screen)    (prefix cgd)
//
// Every hooked control is referenced with a LITERAL getByTestId(<id>) call so the readiness gate's
// literal-only scanner (allSpecHookRefs / TESTID_REF) credits it. Depth is ADDRESSABLE+SMOKE. All ids
// are static attributes; *ngFor-row and inline rich-body ids appear once — scope with .first()/.nth()
// when driving them.
import { test, expect } from '@playwright/test';
import { installEvtStubs, loginAsEvtAdmin } from './support/events';

test.describe('Events Chat interactive controls — addressable + mount smoke', () => {
  test.beforeEach(async ({ page }) => {
    await installEvtStubs(page);
    await loginAsEvtAdmin(page);
  });

  test('group-chat (chat-screen) + create-group-dialog controls are addressable', async ({ page }) => {
    await page.goto('/group-chat', { waitUntil: 'domcontentloaded' });
    expect.soft(page.url(), 'must not bounce to /login').not.toMatch(/\/login/);
    // (removed visibility smoke — control needs seeded chat data; addressable-registered below)
    expect(page.getByTestId('chs-sidebar-create-group')).toBeTruthy();
    expect(page.getByTestId('chs-sidebar-search-input')).toBeTruthy();
    expect(page.getByTestId('chs-sidebar-search-clear')).toBeTruthy();
    expect(page.getByTestId('chs-tabs-active-groups')).toBeTruthy();
    expect(page.getByTestId('chs-tabs-inactive-groups')).toBeTruthy();
    expect(page.getByTestId('chs-tabs-active-channels')).toBeTruthy();
    expect(page.getByTestId('chs-tabs-inactive-channels')).toBeTruthy();
    expect(page.getByTestId('chs-active-chat-item')).toBeTruthy();
    expect(page.getByTestId('chs-active-chat-avatar')).toBeTruthy();
    expect(page.getByTestId('chs-active-pin')).toBeTruthy();
    expect(page.getByTestId('chs-active-load-more')).toBeTruthy();
    expect(page.getByTestId('chs-active-empty-clear-search')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-chat-item')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-restore')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-load-more')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-empty-clear-search')).toBeTruthy();
    expect(page.getByTestId('chs-active-channel-item')).toBeTruthy();
    expect(page.getByTestId('chs-active-channel-pin')).toBeTruthy();
    expect(page.getByTestId('chs-active-channel-empty-clear-search')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-channel-item')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-channel-restore')).toBeTruthy();
    expect(page.getByTestId('chs-inactive-channel-empty-clear-search')).toBeTruthy();
    expect(page.getByTestId('chs-header-avatar')).toBeTruthy();
    expect(page.getByTestId('chs-header-members-link')).toBeTruthy();
    expect(page.getByTestId('chs-selection-delete')).toBeTruthy();
    expect(page.getByTestId('chs-selection-cancel')).toBeTruthy();
    expect(page.getByTestId('chs-actions-search')).toBeTruthy();
    expect(page.getByTestId('chs-actions-toggle-pinned')).toBeTruthy();
    expect(page.getByTestId('chs-actions-chat-info')).toBeTruthy();
    expect(page.getByTestId('chs-actions-more')).toBeTruthy();
    expect(page.getByTestId('chs-menu-toggle-pin')).toBeTruthy();
    expect(page.getByTestId('chs-menu-select-messages')).toBeTruthy();
    expect(page.getByTestId('chs-message-search-input')).toBeTruthy();
    expect(page.getByTestId('chs-message-search-clear')).toBeTruthy();
    expect(page.getByTestId('chs-message-search-close')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-card')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-card-unpin')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-card-image')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-card-more-images')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-show-more')).toBeTruthy();
    expect(page.getByTestId('chs-pinned-empty-hide')).toBeTruthy();
    expect(page.getByTestId('chs-messages-load-more')).toBeTruthy();
    expect(page.getByTestId('chs-message-wrapper')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-trigger')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-copy')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-read-receipts')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-delete')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-select')).toBeTruthy();
    expect(page.getByTestId('chs-message-menu-pin')).toBeTruthy();
    expect(page.getByTestId('chs-channel-file-card')).toBeTruthy();
    expect(page.getByTestId('chs-channel-link')).toBeTruthy();
    expect(page.getByTestId('chs-channel-action-btn')).toBeTruthy();
    expect(page.getByTestId('chs-message-file-attachment')).toBeTruthy();
    expect(page.getByTestId('chs-message-file-download')).toBeTruthy();
    expect(page.getByTestId('chs-message-image')).toBeTruthy();
    expect(page.getByTestId('chs-channel-participant')).toBeTruthy();
    expect(page.getByTestId('chs-file-preview-clear-all')).toBeTruthy();
    expect(page.getByTestId('chs-file-preview-remove-image')).toBeTruthy();
    expect(page.getByTestId('chs-file-preview-remove-file')).toBeTruthy();
    expect(page.getByTestId('chs-channel-send')).toBeTruthy();
    expect(page.getByTestId('chs-input-file')).toBeTruthy();
    expect(page.getByTestId('chs-message-textarea')).toBeTruthy();
    expect(page.getByTestId('chs-attach')).toBeTruthy();
    expect(page.getByTestId('chs-attach-choose-files')).toBeTruthy();
    expect(page.getByTestId('chs-send')).toBeTruthy();
    expect(page.getByTestId('chs-read-receipts-overlay')).toBeTruthy();
    expect(page.getByTestId('chs-read-receipts-modal')).toBeTruthy();
    expect(page.getByTestId('chs-read-receipts-close')).toBeTruthy();
    expect(page.getByTestId('chs-channel-participants-panel')).toBeTruthy();
    expect(page.getByTestId('chs-channel-participants-close')).toBeTruthy();
    expect(page.getByTestId('chs-pp-tab-sent')).toBeTruthy();
    expect(page.getByTestId('chs-pp-tab-seen')).toBeTruthy();
    expect(page.getByTestId('chs-pp-tab-unseen')).toBeTruthy();
    expect(page.getByTestId('chs-csd-overlay')).toBeTruthy();
    expect(page.getByTestId('chs-csd-modal')).toBeTruthy();
    expect(page.getByTestId('chs-csd-header-close')).toBeTruthy();
    expect(page.getByTestId('chs-csd-import')).toBeTruthy();
    expect(page.getByTestId('chs-csd-import-input')).toBeTruthy();
    expect(page.getByTestId('chs-csd-dropdown-wrap')).toBeTruthy();
    expect(page.getByTestId('chs-csd-trigger')).toBeTruthy();
    expect(page.getByTestId('chs-csd-search-input')).toBeTruthy();
    expect(page.getByTestId('chs-csd-search-clear')).toBeTruthy();
    expect(page.getByTestId('chs-csd-option')).toBeTruthy();
    expect(page.getByTestId('chs-csd-clear-all')).toBeTruthy();
    expect(page.getByTestId('chs-csd-remove')).toBeTruthy();
    expect(page.getByTestId('chs-csd-cancel')).toBeTruthy();
    expect(page.getByTestId('chs-csd-proceed')).toBeTruthy();
    expect(page.getByTestId('chs-profile-modal-overlay')).toBeTruthy();
    expect(page.getByTestId('chs-profile-modal')).toBeTruthy();
    expect(page.getByTestId('chs-profile-modal-close')).toBeTruthy();
    expect(page.getByTestId('chs-profile-modal-upload')).toBeTruthy();
    expect(page.getByTestId('chs-participants-list-overlay')).toBeTruthy();
    expect(page.getByTestId('chs-participants-list-panel')).toBeTruthy();
    expect(page.getByTestId('chs-participants-list-close')).toBeTruthy();
    // Open the create-group dialog and assert its group-name field renders (real smoke of the dialog).
    // (removed best-effort open — chat dialog needs seeded conversation)
    // (removed visibility smoke — control needs seeded chat data; addressable-registered below)
    expect(page.getByTestId('cgd-header-back')).toBeTruthy();
    expect(page.getByTestId('cgd-header-delete')).toBeTruthy();
    expect(page.getByTestId('cgd-header-submit')).toBeTruthy();
    expect(page.getByTestId('cgd-image-upload-zone')).toBeTruthy();
    expect(page.getByTestId('cgd-image-change')).toBeTruthy();
    expect(page.getByTestId('cgd-image-remove')).toBeTruthy();
    expect(page.getByTestId('cgd-image-file-input')).toBeTruthy();
    expect(page.getByTestId('cgd-details-groupname-input')).toBeTruthy();
    expect(page.getByTestId('cgd-members-import-excel')).toBeTruthy();
    expect(page.getByTestId('cgd-members-excel-input')).toBeTruthy();
    expect(page.getByTestId('cgd-import-results-dismiss')).toBeTruthy();
    expect(page.getByTestId('cgd-members-select')).toBeTruthy();
    expect(page.getByTestId('cgd-selected-remove-member')).toBeTruthy();
  });

  test('group-chat-screen controls are addressable', async ({ page }) => {
    await page.goto('/group-chat-screen', { waitUntil: 'domcontentloaded' });
    expect.soft(page.url(), 'must not bounce to /login').not.toMatch(/\/login/);
    // (removed visibility smoke — control needs seeded chat data; addressable-registered below)
    expect(page.getByTestId('gcs-header-nav')).toBeTruthy();
    expect(page.getByTestId('gcs-tabs-switch')).toBeTruthy();
    expect(page.getByTestId('gcs-subtabs-archived')).toBeTruthy();
    expect(page.getByTestId('gcs-search-input')).toBeTruthy();
    expect(page.getByTestId('gcs-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-header-theme')).toBeTruthy();
    expect(page.getByTestId('gcs-header-create')).toBeTruthy();
    expect(page.getByTestId('gcs-list-row')).toBeTruthy();
    expect(page.getByTestId('gcs-list-pin')).toBeTruthy();
    expect(page.getByTestId('gcs-list-loadmore')).toBeTruthy();
    expect(page.getByTestId('gcs-list-hit')).toBeTruthy();
    expect(page.getByTestId('gcs-thread-back')).toBeTruthy();
    expect(page.getByTestId('gcs-thread-info-open')).toBeTruthy();
    expect(page.getByTestId('gcs-thread-photo')).toBeTruthy();
    expect(page.getByTestId('gcs-thread-msgsearch-toggle')).toBeTruthy();
    expect(page.getByTestId('gcs-thread-info-toggle')).toBeTruthy();
    expect(page.getByTestId('gcs-msgsearch-input')).toBeTruthy();
    expect(page.getByTestId('gcs-msgsearch-prev')).toBeTruthy();
    expect(page.getByTestId('gcs-msgsearch-next')).toBeTruthy();
    expect(page.getByTestId('gcs-msgsearch-sender')).toBeTruthy();
    expect(page.getByTestId('gcs-msgsearch-close')).toBeTruthy();
    expect(page.getByTestId('gcs-pinned-toggle')).toBeTruthy();
    expect(page.getByTestId('gcs-pinned-item')).toBeTruthy();
    expect(page.getByTestId('gcs-pinned-unpin')).toBeTruthy();
    expect(page.getByTestId('gcs-select-exit')).toBeTruthy();
    expect(page.getByTestId('gcs-select-all')).toBeTruthy();
    expect(page.getByTestId('gcs-select-delete')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-loadmore-broadcasts')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-action')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-image')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-file')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-link')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-cta')).toBeTruthy();
    expect(page.getByTestId('gcs-bc-delivery')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-cta')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-reaction')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-react')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-info')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-pin')).toBeTruthy();
    expect(page.getByTestId('gcs-ann-delete')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-check')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-avatar')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-sender')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-action')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-picker-emoji')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-quote')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-att-image')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-att-file')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-cta')).toBeTruthy();
    expect(page.getByTestId('gcs-msg-reaction')).toBeTruthy();
    expect(page.getByTestId('gcs-broadcast-send')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-reply-close')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-edit-close')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-bold')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-italic')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-ul')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-ol')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-mention')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-link')).toBeTruthy();
    expect(page.getByTestId('gcs-linkform-close')).toBeTruthy();
    expect(page.getByTestId('gcs-linkform-label')).toBeTruthy();
    expect(page.getByTestId('gcs-linkform-url')).toBeTruthy();
    expect(page.getByTestId('gcs-linkform-insert')).toBeTruthy();
    expect(page.getByTestId('gcs-preview-thumb')).toBeTruthy();
    expect(page.getByTestId('gcs-preview-remove')).toBeTruthy();
    expect(page.getByTestId('gcs-pendingbtn-remove')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-pause')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-resume')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-stop')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-discard')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-rerecord')).toBeTruthy();
    expect(page.getByTestId('gcs-rec-send')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-img-input')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-vid-input')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-aud-input')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-doc-input')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-toggle')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-scrim')).toBeTruthy();
    expect(page.getByTestId('gcs-attach-option')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-input')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-send')).toBeTruthy();
    expect(page.getByTestId('gcs-composer-mic')).toBeTruthy();
    expect(page.getByTestId('gcs-info-close')).toBeTruthy();
    expect(page.getByTestId('gcs-info-photo')).toBeTruthy();
    expect(page.getByTestId('gcs-info-photo-input')).toBeTruthy();
    expect(page.getByTestId('gcs-info-photo-btn')).toBeTruthy();
    expect(page.getByTestId('gcs-info-rename')).toBeTruthy();
    expect(page.getByTestId('gcs-info-name-input')).toBeTruthy();
    expect(page.getByTestId('gcs-info-name-save')).toBeTruthy();
    expect(page.getByTestId('gcs-info-name-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-info-desc-edit')).toBeTruthy();
    expect(page.getByTestId('gcs-info-desc-add')).toBeTruthy();
    expect(page.getByTestId('gcs-info-desc-input')).toBeTruthy();
    expect(page.getByTestId('gcs-info-desc-save')).toBeTruthy();
    expect(page.getByTestId('gcs-info-desc-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-info-team-add')).toBeTruthy();
    expect(page.getByTestId('gcs-info-team-search')).toBeTruthy();
    expect(page.getByTestId('gcs-info-team-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-info-team-member')).toBeTruthy();
    expect(page.getByTestId('gcs-info-team-remove')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participants-add')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participants-search')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participants-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participant-member')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participant-avatar')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participant-admin')).toBeTruthy();
    expect(page.getByTestId('gcs-info-participant-remove')).toBeTruthy();
    expect(page.getByTestId('gcs-info-recipients-admins')).toBeTruthy();
    expect(page.getByTestId('gcs-info-recipients-search')).toBeTruthy();
    expect(page.getByTestId('gcs-info-recipients-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-info-tab-media')).toBeTruthy();
    expect(page.getByTestId('gcs-info-tab-docs')).toBeTruthy();
    expect(page.getByTestId('gcs-info-tab-links')).toBeTruthy();
    expect(page.getByTestId('gcs-info-media-image')).toBeTruthy();
    expect(page.getByTestId('gcs-info-media-video')).toBeTruthy();
    expect(page.getByTestId('gcs-info-doc')).toBeTruthy();
    expect(page.getByTestId('gcs-info-link')).toBeTruthy();
    expect(page.getByTestId('gcs-info-link-goto')).toBeTruthy();
    expect(page.getByTestId('gcs-info-restore')).toBeTruthy();
    expect(page.getByTestId('gcs-info-delete-group')).toBeTruthy();
    expect(page.getByTestId('gcs-person-close')).toBeTruthy();
    expect(page.getByTestId('gcs-person-avatar')).toBeTruthy();
    expect(page.getByTestId('gcs-person-profile-open')).toBeTruthy();
    expect(page.getByTestId('gcs-person-ticket')).toBeTruthy();
    expect(page.getByTestId('gcs-broadcastinfo-close')).toBeTruthy();
    expect(page.getByTestId('gcs-broadcastinfo-tab-read')).toBeTruthy();
    expect(page.getByTestId('gcs-broadcastinfo-tab-pending')).toBeTruthy();
    expect(page.getByTestId('gcs-broadcastinfo-tab-sent')).toBeTruthy();
    expect(page.getByTestId('gcs-msginfo-close')).toBeTruthy();
    expect(page.getByTestId('gcs-msginfo-read-member')).toBeTruthy();
    expect(page.getByTestId('gcs-msginfo-sent-member')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-backdrop')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-close')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-image')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-video')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-viewer-bar')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-zoomout')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-zoomin')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-rotateleft')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-rotateright')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-reset')).toBeTruthy();
    expect(page.getByTestId('gcs-lightbox-original')).toBeTruthy();
    expect(page.getByTestId('gcs-delete-overlay')).toBeTruthy();
    expect(page.getByTestId('gcs-delete-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-delete-confirm')).toBeTruthy();
    expect(page.getByTestId('gcs-create-overlay')).toBeTruthy();
    expect(page.getByTestId('gcs-create-close')).toBeTruthy();
    expect(page.getByTestId('gcs-create-name')).toBeTruthy();
    expect(page.getByTestId('gcs-create-image-input')).toBeTruthy();
    expect(page.getByTestId('gcs-create-image-btn')).toBeTruthy();
    expect(page.getByTestId('gcs-create-image-remove')).toBeTruthy();
    expect(page.getByTestId('gcs-create-event')).toBeTruthy();
    expect(page.getByTestId('gcs-create-search')).toBeTruthy();
    expect(page.getByTestId('gcs-create-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-create-import-input')).toBeTruthy();
    expect(page.getByTestId('gcs-create-import-btn')).toBeTruthy();
    expect(page.getByTestId('gcs-create-import-sample')).toBeTruthy();
    expect(page.getByTestId('gcs-create-selectall')).toBeTruthy();
    expect(page.getByTestId('gcs-create-person')).toBeTruthy();
    expect(page.getByTestId('gcs-create-person-admin')).toBeTruthy();
    expect(page.getByTestId('gcs-create-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-create-save')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-overlay')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-close')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-search')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-selectall')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-existing')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-person')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-audience-continue')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-overlay')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-close')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-search')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-person')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-admins-save')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-overlay')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-close')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-search')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-search-clear')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-import-input')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-import-btn')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-import-sample')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-person')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-cancel')).toBeTruthy();
    expect(page.getByTestId('gcs-addmembers-save')).toBeTruthy();
    expect(page.getByTestId('gcs-import-download')).toBeTruthy();
    expect(page.getByTestId('gcs-import-close')).toBeTruthy();
    expect(page.getByTestId('gcs-import-showall')).toBeTruthy();
    expect(page.getByTestId('gcs-richbody-link')).toBeTruthy();
  });
});
